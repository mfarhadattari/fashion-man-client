import SectionTitle from "../../../components/SectionTitle";

const PaymentHistory = () => {
  return (
    <main>
      <section className="my-10">
        <SectionTitle title="Payment History" subtitle="See Your Payment!" />
      </section>
    </main>
  );
};

export default PaymentHistory;
